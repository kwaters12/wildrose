.row
  .col-md-3
    = link_to image_tag("logo1.png"), root_url
  .col-md-9.navUserItems
    %ul
      %li 
        Shopping Cart
      %li
        | Account
      %li 
        | Logout
%ul.categories
  - if @categories
    - @categories.each do |category|
      %li{:id => dom_id(category)}
        = link_to "#{category.title}", "shop"
